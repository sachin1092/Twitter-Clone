<!DOCTYPE html>
<html>
	<head>
		<title><%= title %></title>
  		<%= render 'layouts/stylesheets' %>
	</head>
	<body>
		<div class="container">
			<%= render 'layouts/header' %>
			<div class="jumbotron">
				<%= yield %>
			</div>
			<%= render 'layouts/footer' %>
		</div>
		<%= render 'layouts/scripts' %>
	</body>
</html>